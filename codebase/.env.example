# Optional transcription language. Leave blank for automatic detection.
LANGUAGE=

# Optional interface language (for example: en, es, fr, de, pt, it).
UI_LANGUAGE=en

# Optional local diagnostic verbosity.
MNEMORA_LOG_LEVEL=info
